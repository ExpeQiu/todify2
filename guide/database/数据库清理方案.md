# Todify4 数据库清理方案

**生成时间**: 2025-01-13  
**目标**: 清理无用的数据库表定义和代码，让数据库更加清爽

---

## 一、清理分析结果

### 1.1 未使用的表（Schema中定义但实际不存在）

这些表在 `unified-database-schema-v3.sql` 中定义，但实际数据库中不存在，且采用JSON存储策略：

| 表名 | 状态 | 清理建议 |
|------|------|---------|
| `tech_packaging_materials` | ❌ 不存在 | 从Schema中移除 |
| `tech_promotion_strategies` | ❌ 不存在 | 从Schema中移除 |
| `tech_press_releases` | ❌ 不存在 | 从Schema中移除 |
| `tech_speeches` | ❌ 不存在 | 从Schema中移除 |
| `promotion_tech_points` | ❌ 不存在 | 从Schema中移除 |
| `press_tech_points` | ❌ 不存在 | 从Schema中移除 |
| `speech_tech_points` | ❌ 不存在 | 从Schema中移除 |
| `tech_packaging_conversations` | ❌ 不存在 | 从Schema中移除 |
| `tech_packaging_sources` | ❌ 不存在 | 从Schema中移除 |
| `tech_promotion_conversations` | ❌ 不存在 | 从Schema中移除 |
| `tech_promotion_sources` | ❌ 不存在 | 从Schema中移除 |
| `tech_press_conversations` | ❌ 不存在 | 从Schema中移除 |
| `tech_press_sources` | ❌ 不存在 | 从Schema中移除 |

**原因**: 这些内容采用JSON存储策略，存储在 `workflow_executions.outputs` 字段中。

### 1.2 空表（存在但无数据）

| 表名 | 记录数 | 清理建议 |
|------|-------|---------|
| `ai_search_outputs` | 0 | ⚠️ 保留（可能未来使用） |
| `article_type_ai_roles` | 0 | ⚠️ 保留（配置表） |
| `car_series` | 0 | ⚠️ 保留（基础数据表） |
| `knowledge_usage_logs` | 0 | ⚠️ 保留（统计表） |
| `project_files` | 0 | ⚠️ 保留（关联表，可能使用） |
| `project_knowledge_points` | 0 | ⚠️ 保留（关联表，可能使用） |
| `project_sources` | 0 | ⚠️ 保留（关联表，可能使用） |
| `tech_point_knowledge_points` | 0 | ⚠️ 保留（关联表，可能使用） |
| `workflow_stats_summary` | 0 | ⚠️ 保留（统计汇总表） |
| `workflow_templates` | 0 | ⚠️ 保留（模板表，可能使用） |

**建议**: 这些表虽然当前无数据，但都是功能表，应该保留。

### 1.3 代码中的遗留引用

**问题**: 以下文件中有对不存在表的查询：

1. `backend/src/models/TechPackagingMaterial.ts` - 查询 `tech_packaging_materials`
2. `backend/src/models/TechPromotionStrategy.ts` - 查询 `tech_promotion_strategies`
3. `backend/src/models/TechPressRelease.ts` - 查询 `tech_press_releases`
4. `backend/src/models/Project.ts` - 查询上述表
5. `backend/src/models/TechPoint.ts` - 查询上述表

**建议**: 注释或移除这些查询代码，因为实际数据存储在 `workflow_executions.outputs` 中。

---

## 二、清理执行方案

### 2.1 Schema清理（高优先级）

#### ✅ 步骤1: 创建清理后的Schema文件

**操作**: 从 `unified-database-schema-v3.sql` 中移除以下表的定义：

1. `tech_packaging_materials` (第174-192行)
2. `tech_promotion_strategies` (第195-213行)
3. `tech_press_releases` (第216-236行)
4. `tech_speeches` (第239-258行)
5. `promotion_tech_points` (第261-270行)
6. `press_tech_points` (第273-282行)
7. `speech_tech_points` (第285-294行)
8. `tech_packaging_conversations` (第845-854行)
9. `tech_packaging_sources` (第857-866行)
10. `tech_promotion_conversations` (第869-878行)
11. `tech_promotion_sources` (第881-890行)
12. `tech_press_conversations` (第893-902行)
13. `tech_press_sources` (第905-914行)

**替换为**: 添加注释说明这些内容采用JSON存储策略。

### 2.2 索引清理（中优先级）

#### ✅ 步骤2: 从索引文件中移除相关索引

**操作**: 从 `unified-database-indexes-v3.sql` 中移除以下索引定义：

- `tech_packaging_materials` 相关索引
- `tech_promotion_strategies` 相关索引
- `tech_press_releases` 相关索引
- `tech_speeches` 相关索引
- 关联表的索引

### 2.3 代码清理（中优先级）

#### ⚠️ 步骤3: 清理模型文件中的查询代码

**操作**: 在以下文件中注释或移除对不存在表的查询：

1. `backend/src/models/TechPackagingMaterial.ts`
2. `backend/src/models/TechPromotionStrategy.ts`
3. `backend/src/models/TechPressRelease.ts`
4. `backend/src/models/Project.ts` (相关方法)
5. `backend/src/models/TechPoint.ts` (相关方法)

**建议**: 添加注释说明数据实际存储在 `workflow_executions.outputs` 中。

---

## 三、清理后的Schema结构

### 3.1 保留的核心表（41个表）

#### 基础数据层（5个）
- brands
- car_models
- car_series
- tech_categories
- tech_points

#### 关联关系层（2个）
- tech_point_car_models
- tech_point_knowledge_points

#### AI内容生成层（2个）
- knowledge_points
- tech_point_knowledge_points（已在关联层）

#### 工作流与对话层（5个）
- agent_workflows
- workflow_executions（统一表，包含所有AI生成内容）
- workflow_templates
- conversations
- chat_messages

#### 配置与统计层（9个）
- ai_roles
- public_page_configs
- page_tool_configs
- files
- workflow_node_usage
- ai_qa_feedback
- workflow_session_stats
- node_content_processing
- workflow_stats_summary

#### 项目管理层（6个）
- projects
- project_sources
- project_tech_points
- project_knowledge_points
- project_files
- project_source_informations

#### 来源信息层（1个）
- source_information

#### 知识库层（2个）
- public_knowledge_categories
- public_knowledge_files

#### 其他功能表（9个）
- ai_search_conversations
- ai_search_messages
- ai_search_outputs
- ai_search_field_mappings
- article_types
- article_type_ai_roles
- brainstorm_sessions
- brainstorm_participants
- brainstorm_messages
- resources
- tech_point_resources
- knowledge_usage_logs
- id_mappings
- database_migrations

**总计**: 约 **41个表**（清理后）

### 3.2 移除的表（13个）

- tech_packaging_materials
- tech_promotion_strategies
- tech_press_releases
- tech_speeches
- promotion_tech_points
- press_tech_points
- speech_tech_points
- tech_packaging_conversations
- tech_packaging_sources
- tech_promotion_conversations
- tech_promotion_sources
- tech_press_conversations
- tech_press_sources

---

## 四、执行步骤

### 4.1 立即执行

1. ✅ 创建清理后的Schema文件 `unified-database-schema-v4.sql`
2. ✅ 创建清理后的索引文件 `unified-database-indexes-v4.sql`
3. ✅ 更新代码中的模型文件

### 4.2 验证步骤

1. ✅ 执行数据库验证脚本
2. ✅ 确认所有功能正常
3. ✅ 更新文档

---

## 五、注意事项

### 5.1 ⚠️ 重要提醒

1. **备份数据库**: 清理前必须备份数据库
2. **测试环境**: 先在测试环境验证
3. **代码兼容**: 确保代码修改后不影响现有功能
4. **文档更新**: 更新相关文档说明

### 5.2 未来扩展

如果未来需要内容管理功能（搜索、版本控制、审批流程），可以：
1. 重新创建这些表
2. 从 `workflow_executions.outputs` 迁移数据
3. 更新代码使用独立表

---

## 六、清理收益

### 6.1 清理效果

- **Schema文件**: 减少约300行代码
- **索引文件**: 减少约50行代码
- **代码文件**: 清理约200行无用查询代码
- **数据库表**: 从41个表减少到约28个核心表（概念上）

### 6.2 维护收益

- ✅ Schema更清晰，易于理解
- ✅ 减少维护成本
- ✅ 避免混淆（表存在但无数据）
- ✅ 代码更简洁

---

**清理方案生成时间**: 2025-01-13  
**执行状态**: 待执行  
**优先级**: 高（建议立即执行）
