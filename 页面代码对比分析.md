# 三个页面代码差异化对比分析

## 一、页面组件代码对比

### 1. TechStrategyPage.tsx (技术策略页面)
```tsx
import React from "react";
import BaseAISearchPage from "../components/ai-search/BaseAISearchPage";
import { techStrategyConfig } from "../configs/pageConfigs";

const TechStrategyPage: React.FC = () => {
  return <BaseAISearchPage config={techStrategyConfig} />;
};

export default TechStrategyPage;
```

### 2. TechArticlePage.tsx (技术通稿页面)
```tsx
import React from "react";
import BaseAISearchPage from "../components/ai-search/BaseAISearchPage";
import { techArticleConfig } from "../configs/pageConfigs";

const TechArticlePage: React.FC = () => {
  return <BaseAISearchPage config={techArticleConfig} />;
};

export default TechArticlePage;
```

### 3. TechPackagePage.tsx (技术包装页面)
```tsx
/**
 * 技术包装页面
 * 使用技术包装配置的独立页面组件
 */
import React from "react";
import BaseAISearchPage from "../components/ai-search/BaseAISearchPage";
import { techPackageConfig } from "../configs/pageConfigs";

const TechPackagePage: React.FC = () => {
  return <BaseAISearchPage config={techPackageConfig} />;
};

export default TechPackagePage;
```

**结论：三个页面组件代码结构完全相同，都使用 `BaseAISearchPage` 组件，仅传入不同的配置对象。**

---

## 二、配置文件差异对比

### 配置项对比表

| 配置项 | tech-package | tech-strategy | tech-article |
|--------|-------------|---------------|--------------|
| **pageType** | `'tech-package'` | `'tech-strategy'` | `'tech-article'` |
| **pageTitle** | `'技术包装'` | `'技术策略'` | `'技术通稿'` |
| **dialogueTitle** | `'AI内容助手'` | `'AI内容助手'` | `'AI内容助手'` |
| **studioTitle** | `'更多工具箱'` | `'更多工具箱'` | `'更多工具箱'` |
| **workflowSelectionKey** | `'ai-search.workflows.selection.tech-package'` | `'ai-search.workflows.selection.tech-strategy'` | `'ai-search.workflows.selection.tech-article'` |

### featureLabelMap 差异对比

| 工具ID | tech-package | tech-strategy | tech-article |
|--------|-------------|---------------|--------------|
| `five-view-analysis` | **"五看"** | "技术转译" | "技术转译" |
| `three-fix-analysis` | **"三定"** | "用户场景挖掘" | "用户场景挖掘" |
| `tech-matrix` | "技术矩阵" | "技术矩阵" | "技术矩阵" |
| `propagation-strategy` | **"传播"** | "传播策略" | "传播策略" |
| `exhibition-video` | "展具与视频" | "展具与视频" | "展具与视频" |
| `translation` | "翻译" | "翻译" | "翻译" |
| `ppt-outline` | "技术讲稿" | "技术讲稿" | "技术讲稿" |
| `script` | "脚本" | "脚本" | "脚本" |

**差异点：**
- `tech-package` 使用简化的标签："五看"、"三定"、"传播"
- `tech-strategy` 和 `tech-article` 使用更详细的标签："技术转译"、"用户场景挖掘"、"传播策略"

### enabledToolIds 差异对比（核心差异）

#### tech-package（技术包装）
```typescript
enabledToolIds: [
  'five-view-analysis',      // ✅ 启用
  'three-fix-analysis',      // ✅ 启用
  'tech-matrix',             // ✅ 启用
  'propagation-strategy',    // ✅ 启用
  'exhibition-video',        // ✅ 启用
  'translation',             // ✅ 启用
  'ppt-outline',             // ✅ 启用
  'script',                  // ✅ 启用
]
```
**启用所有 8 个工具**

#### tech-strategy（技术策略）
```typescript
enabledToolIds: [
  'propagation-strategy',    // ✅ 启用
  'five-view-analysis',      // ✅ 启用
  'three-fix-analysis',      // ✅ 启用
  'translation',             // ✅ 启用
  // ❌ 未启用: 'tech-matrix'
  // ❌ 未启用: 'exhibition-video'
  // ❌ 未启用: 'ppt-outline'
  // ❌ 未启用: 'script'
]
```
**仅启用 4 个工具：传播策略、技术转译、用户场景挖掘、翻译**

#### tech-article（技术通稿）
```typescript
enabledToolIds: [
  'ppt-outline',             // ✅ 启用
  'translation',             // ✅ 启用
  // ❌ 未启用: 'five-view-analysis'
  // ❌ 未启用: 'three-fix-analysis'
  // ❌ 未启用: 'tech-matrix'
  // ❌ 未启用: 'propagation-strategy'
  // ❌ 未启用: 'exhibition-video'
  // ❌ 未启用: 'script'
]
```
**仅启用 2 个工具：技术讲稿、翻译**

---

## 三、总结

### 代码结构
- **完全相同**：三个页面组件都使用相同的实现方式，只是传入不同的配置

### 主要差异

1. **页面标题不同**
   - tech-package: "技术包装"
   - tech-strategy: "技术策略"
   - tech-article: "技术通稿"

2. **工具标签显示不同**
   - tech-package 使用简化标签（"五看"、"三定"、"传播"）
   - tech-strategy 和 tech-article 使用详细标签（"技术转译"、"用户场景挖掘"、"传播策略"）

3. **可用工具数量差异（最重要）**
   - **tech-package**: 8 个工具全部启用（功能最全）
   - **tech-strategy**: 4 个工具启用（聚焦传播策略和技术转译）
   - **tech-article**: 2 个工具启用（仅技术讲稿和翻译，最精简）

4. **工作流选择键不同**
   - 每个页面使用不同的 localStorage 键来保存工作流选择偏好

### 设计模式
三个页面采用了**配置驱动**的设计模式：
- 共享同一个基础组件 `BaseAISearchPage`
- 通过不同的配置对象来定制页面行为和功能
- 实现了代码复用和功能差异化
